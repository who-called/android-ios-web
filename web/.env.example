# Public API base (the Express backend). Override per environment.
NEXT_PUBLIC_API_BASE_URL=https://api.who-called.com/api/v1

# Canonical site URL (used for SEO metadata, sitemap, OG).
NEXT_PUBLIC_SITE_URL=https://www.who-called.com
